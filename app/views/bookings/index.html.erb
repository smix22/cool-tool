<h1>Bookings</h1>


<div class="cards">
  <% @bookings.each do |booking| %>
    <div class="card-category" style="">
      <%= booking.request%><br>
      <%= "Start date" %>
      <%= booking.start_date %><br>
      <%= "End date" %>
      <%= booking.end_date %><br>
      <%= link_to "🗑", bookings_path(booking), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%><br>
       <%=  %><br>
    </div>
  <% end %>
</div>
